#include <stdio.h>

/**
 * main - generate the alphabet like a list in reverse
 * Return: Sucess
 */

int main(void)
{

int za;
for (za = 'z'; za <= 'a'; za--)
putchar (za);
putchar ('\n');
return (0);
}
