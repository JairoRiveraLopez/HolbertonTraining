#include "main.h"

/**
 * add - add two integers
 * @a: integer 1
 * @b: integer 2
 * Return: result of the operation
 */

int add(int a, int b)
{
int r;
r = (a + b);
{
_putchar (a + b);
return (r);
}
}
