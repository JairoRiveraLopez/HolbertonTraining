#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: is one integer
 * @b: is one integer
 * Return: 1 if lowercase, 0 if not.
 */

int mul(int a, int b)

{
return (a * b);
}
